services:
  db:
    image: postgres:15-alpine
    container_name: ganaqro_db_local
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-ganaqro_local}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    volumes:
      - postgres_data_local:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "5433:5432"  # Expose on different port to avoid conflicts

  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile-local
    container_name: ganaqro_web_local
    env_file:
      - .env
    command: gunicorn --bind 0.0.0.0:8000 --workers 1 --timeout 5 --chdir ganaqro ganaqro.wsgi_local:application
    volumes:
      - ..:/app                                  # entire project mount
      - ../ganaqro/staticfiles:/app/ganaqro/staticfiles  # collectstatic mount
      - ../ganaqro/media:/app/ganaqro/media               # media mount
    ports:
      - "8000:8000"  # Expose directly for local development
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-ganaqro_local}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_HOST: ${POSTGRES_HOST:-db}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      DEBUG: "True"
      SECRET_KEY: ${SECRET_KEY:-django-insecure-local-dev-key-change-in-production}
      ALLOWED_HOSTS: ${ALLOWED_HOSTS:-localhost,127.0.0.1}
      ADMIN_URL: ${ADMIN_URL:-admin/}
      EMAIL_HOST: ${EMAIL_HOST:-smtp.gmail.com}
      EMAIL_PORT: ${EMAIL_PORT:-587}
      EMAIL_USE_TLS: ${EMAIL_USE_TLS:-True}
      EMAIL_HOST_USER: ${EMAIL_HOST_USER:-}
      EMAIL_HOST_PASSWORD: ${EMAIL_HOST_PASSWORD:-}
      DEFAULT_FROM_EMAIL: ${DEFAULT_FROM_EMAIL:-}
      CONTACT_RECEIVER_EMAIL: ${CONTACT_RECEIVER_EMAIL:-}
      TURNSTILE_SITE_KEY: ${TURNSTILE_SITE_KEY:-}
      TURNSTILE_SECRET_KEY: ${TURNSTILE_SECRET_KEY:-}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data_local:

